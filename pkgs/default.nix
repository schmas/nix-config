# Custom packages, that can be defined similarly to ones from nixpkgs
# You can build them using 'nix build .#example'
# Ref: https://github.com/Misterio77/nix-config/tree/main/pkgs
pkgs: {
  # example = pkgs.callPackage ./example { };
}
